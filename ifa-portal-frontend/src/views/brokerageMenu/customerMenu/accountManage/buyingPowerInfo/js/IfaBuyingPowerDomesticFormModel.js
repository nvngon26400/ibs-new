export class IfaBuyingPowerDomesticFormModel {
  constructor() {
    this.title = '買付余力(国内)'
    this.accountTypeRadio = '2' // 口座区分 【初期値】全て
    // 買付余力
    this.settlementDateT2 = '' // 買付余力一覧.受渡日(T+2)
    this.settlementDateT3 = '' // 買付余力一覧.受渡日(T+3)
    this.settlementDateT4 = '' // 買付余力一覧.受渡日(T+4)
    this.yenBuyingPowerGeneralAccountT2 = 0 // 買付余力一覧.買付余力(T+2)
    this.yenBuyingPowerGeneralAccountT3 = 0 // 買付余力一覧.買付余力(T+3)
    this.yenBuyingPowerGeneralAccountT4 = 0 // 買付余力一覧.買付余力(T+4)
    this.yenBuyingPowerJrNisaT2 = 0 // 買付余力一覧.買付余力（ジュニアNISA口座）(T+2)
    this.yenBuyingPowerJrNisaT3 = 0 // 買付余力一覧.買付余力（ジュニアNISA口座）(T+3)
    this.yenBuyingPowerJrNisaT4 = 0 // 買付余力一覧.買付余力（ジュニアNISA口座）(T+4)
    this.buyingPower = '※買付余力は取引内容、商品受渡日別に算出されます。（投資信託の受渡日は各ファンド毎に異なります。）' // 買付余力注釈
    // 買付余力対象商品残高
    this.totalOfMrfDpositMargin = '' // MRF・預り金・信用保証金
    this.depositAmount = '' // 入金額
    this.totalAmount = '' // 合計額
    this.nisatotalOfMrfDpositMargin = '' // MRF・預り金・信用保証金（JrNisa）
    this.nisadepositAmount = '' // 入金額（JrNisa）
    this.nisatotalAmount = '' // 合計額（JrNisa）
    this.pointShubetsu = '' // ポイント.ポイント種別
    this.pointNumber = '' // ポイント.ポイント数
    this.fixedTermPoint = '' // ポイント.うち期間固定ポイント
    this.pointShortLimit = '' // ポイント.最短有効期限
    this.pointDisplayAreaAvailability = '' // ポイント.ポイント表示エリア表示可否
    this.pointNameDisplayAvailability = '' // ポイント.ポイント名表示可否
    this.pointNumberDisplayAvailability = '' // ポイント.ポイント数表示可否
    this.fixedTermPointDisplayAvailability = '' // ポイント.うち期間固定ポイント表示可否
    this.pointShortLimitDisplayAvailability = '' // ポイント.最短有効期限表示可否
    // 精算予定一覧表（当日）
    this.todaySettlementDate = '' // 当日（受渡日）
    this.todayDepositIncludingMrf = 0 // 当日（預り金(MRF含む)）
    this.todayMargin = 0 // 当日（保証金現金）
    this.todayYenDeposit = 0 // 当日（預り金）
    this.todayDepositAmount = 0 // 当日（入金額）
    this.todayPayment = 0 // 当日（支払額）
    this.todayUncontractedCreditSettlementLoss = 0 // 当日（未約定信用決済損）
    this.todayUncontractedPurchaseOrderAmount = 0 // 当日（未約定買注文額）
    this.todayWithdrawalTransferInstructions = 0 // 当日（出金・振替指示額）
    this.todayAmountReceived = 0 // 当日（受取額）
    this.todayDayTrading = 0 // 当日（受取額（日計り分））
    this.todaySbiHybridDepositSettlementAmount = 0 // 当日(SBIハイブリッド預金精算額)
    this.todayTotalBalance = 0 // 当日（残高合計額）
    this.todayOurCompanyTotalBalance = 0 // 当日(残高（当社）)
    this.todaySbiHybridDepositSettlementTotalBalance = 0 // 当日(残高(SBIハイブリッド預金))
    this.todaySbiHybridDepositSettlementRestrictedAmount = 0 // 当日(SBIハイブリッド預金拘束額)
    this.todayMarginTotalBalance = 0 // 当日(残高(保証金現金))
    this.todayDpositTotalBalance = 0 // 当日(残高(預り金))
    this.todayRequiredSettlementAmountYen = 0 // 当日（必要精算額、必要保証金額）
    this.todayYenBuyingPowerGeneralAccount = 0 // 当日（買付余力）
    // 精算予定一覧表（1営業日後）
    this.afterOnedaySettlementDate = '' // 1営業日後（受渡日）
    this.afterOnedayDepositIncludingMrf = 0 // 1営業日後（預り金(MRF含む)）
    this.afterOnedayMargin = 0 // 1営業日後（保証金現金）
    this.afterOnedayYenDeposit = 0 // 1営業日後（預り金）
    this.afterOnedayDepositAmount = 0 // 1営業日後（入金額）
    this.afterOnedayPayment = 0 // 1営業日後（支払額）
    this.afterOnedayUncontractedCreditSettlementLoss = 0 // 1営業日後（未約定信用決済損）
    this.afterOnedayUncontractedPurchaseOrderAmount = 0 // 1営業日後（未約定買注文額）
    this.afterOnedayWithdrawalTransferInstructions = 0 // 1営業日後（出金・振替指示額）
    this.afterOnedayAmountReceived = 0 // 1営業日後（受取額）
    this.afterOnedayDayTrading = 0 // 1営業日後（受取額（日計り分））
    this.afterOnedaySbiHybridDepositSettlementAmount = 0 // 1営業日後(SBIハイブリッド預金精算額)
    this.afterOnedayTotalBalance = 0 // 1営業日後（残高合計額）
    this.afterOnedayOurCompanyTotalBalance = 0 // 1営業日後(残高（当社）)
    this.afterOnedaySbiHybridDepositSettlementTotalBalance = 0 // 1営業日後(残高(SBIハイブリッド預金))
    this.afterOnedaySbiHybridDepositSettlementRestrictedAmount = 0 // 1営業日後(SBIハイブリッド預金拘束額)
    this.afterOnedayMarginTotalBalance = 0 // 1営業日後(残高(保証金現金))
    this.afterOnedayDpositTotalBalance = 0 // 1営業日後(残高(預り金))
    this.afterOnedayRequiredSettlementAmountYen = 0 // 1営業日後（必要精算額、必要保証金額）
    this.afterOnedayYenBuyingPowerGeneralAccount = 0 // 1営業日後（買付余力）
    // 精算予定一覧表（2営業日後）
    this.afterTwodaysSettlementDate = '' // 2営業日後（受渡日）
    this.afterTwodaysDepositIncludingMrf = 0 // 2営業日後（預り金(MRF含む)）
    this.afterTwodaysMargin = 0 // 2営業日後（保証金現金）
    this.afterTwodaysYenDeposit = 0 // 2営業日後（預り金）
    this.afterTwodaysDepositAmount = 0 // 2営業日後（入金額）
    this.afterTwodaysPayment = 0 // 2営業日後（支払額）
    this.afterTwodaysUncontractedCreditSettlementLoss = 0 // 2営業日後（未約定信用決済損）
    this.afterTwodaysUncontractedPurchaseOrderAmount = 0 // 2営業日後（未約定買注文額）
    this.afterTwodaysWithdrawalTransferInstructions = 0 // 2営業日後（出金・振替指示額）
    this.afterTwodaysAmountReceived = 0 // 2営業日後（受取額）
    this.afterTwodaysDayTrading = 0 // 2営業日後（受取額（日計り分））
    this.afterTwodaysSbiHybridDepositSettlementAmount = 0 // 2営業日後(SBIハイブリッド預金精算額)
    this.afterTwodaysTotalBalance = 0 // 2営業日後（残高合計額）
    this.afterTwodaysOurCompanyTotalBalance = 0 // 2営業日後(残高（当社）)
    this.afterTwodaysSbiHybridDepositSettlementTotalBalance = 0 // 2営業日後(残高(SBIハイブリッド預金))
    this.afterTwodaysSbiHybridDepositSettlementRestrictedAmount = 0 // 2営業日後(SBIハイブリッド預金拘束額)
    this.afterTwodaysMarginTotalBalance = 0 // 2営業日後(残高(保証金現金))
    this.afterTwodaysDpositTotalBalance = 0 // 2営業日後(残高(預り金))
    this.afterTwodaysRequiredSettlementAmountYen = 0 // 2営業日後（必要精算額、必要保証金額）
    this.afterTwodaysYenBuyingPowerGeneralAccount = 0 // 2営業日後（買付余力）
    // 精算予定一覧表（3営業日後）
    this.afterThreedaysSettlementDate = '' // 3営業日後（受渡日）
    this.afterThreedaysDepositIncludingMrf = 0 // 3営業日後（預り金(MRF含む)）
    this.afterThreedaysMargin = 0 // 3営業日後（保証金現金）
    this.afterThreedaysYenDeposit = 0 // 3営業日後（預り金）
    this.afterThreedaysDepositAmount = 0 // 3営業日後（入金額）
    this.afterThreedaysPayment = 0 // 3営業日後（支払額）
    this.afterThreedaysUncontractedCreditSettlementLoss = 0 // 3営業日後（未約定信用決済損）
    this.afterThreedaysUncontractedPurchaseOrderAmount = 0 // 3営業日後（未約定買注文額）
    this.afterThreedaysWithdrawalTransferInstructions = 0 // 3営業日後（出金・振替指示額）
    this.afterThreedaysAmountReceived = 0 // 3営業日後（受取額）
    this.afterThreedaysDayTrading = 0 // 3営業日後（受取額（日計り分））
    this.afterThreedaysSbiHybridDepositSettlementAmount = 0 // 3営業日後(SBIハイブリッド預金精算額)
    this.afterThreedaysTotalBalance = 0 // 3営業日後（残高合計額）
    this.afterThreedaysOurCompanyTotalBalance = 0 // 3営業日後(残高（当社）)
    this.afterThreedaysSbiHybridDepositSettlementTotalBalance = 0 // 3営業日後(残高(SBIハイブリッド預金))
    this.afterThreedaysSbiHybridDepositSettlementRestrictedAmount = 0 // 3営業日後(SBIハイブリッド預金拘束額)
    this.afterThreedaysMarginTotalBalance = 0 // 3営業日後(残高(保証金現金))
    this.afterThreedaysDpositTotalBalance = 0 // 3営業日後(残高(預り金))
    this.afterThreedaysRequiredSettlementAmountYen = 0 // 3営業日後（必要精算額、必要保証金額）
    this.afterThreedaysYenBuyingPowerGeneralAccount = 0 // 3営業日後（買付余力）
    // 精算予定一覧表（4営業日後）
    this.afterFourdaysSettlementDate = '' // 4営業日後（受渡日）
    this.afterFourdaysDepositIncludingMrf = 0 // 4営業日後（預り金(MRF含む)）
    this.afterFourdaysMargin = 0 // 4営業日後（保証金現金）
    this.afterFourdaysYenDeposit = 0 // 4営業日後（預り金）
    this.afterFourdaysDepositAmount = 0 // 4営業日後（入金額）
    this.afterFourdaysPayment = 0 // 4営業日後（支払額）
    this.afterFourdaysUncontractedCreditSettlementLoss = 0 // 4営業日後（未約定信用決済損）
    this.afterFourdaysUncontractedPurchaseOrderAmount = 0 // 4営業日後（未約定買注文額）
    this.afterFourdaysWithdrawalTransferInstructions = 0 // 4営業日後（出金・振替指示額）
    this.afterFourdaysAmountReceived = 0 // 4営業日後（受取額）
    this.afterFourdaysDayTrading = 0 // 4営業日後（受取額（日計り分））
    this.afterFourdaysSbiHybridDepositSettlementAmount = 0 // 4営業日後(SBIハイブリッド預金精算額)
    this.afterFourdaysTotalBalance = 0 // 4営業日後（残高合計額）
    this.afterFourdaysOurCompanyTotalBalance = 0 // 4営業日後(残高（当社）)
    this.afterFourdaysSbiHybridDepositSettlementTotalBalance = 0 // 4営業日後(残高(SBIハイブリッド預金))
    this.afterFourdaysSbiHybridDepositSettlementRestrictedAmount = 0 // 4営業日後(SBIハイブリッド預金拘束額)
    this.afterFourdaysMarginTotalBalance = 0 // 4営業日後(残高(保証金現金))
    this.afterFourdaysDpositTotalBalance = 0 // 4営業日後(残高(預り金))
    this.afterFourdaysRequiredSettlementAmountYen = 0 // 4営業日後（必要精算額、必要保証金額）
    this.afterFourdaysYenBuyingPowerGeneralAccount = 0 // 4営業日後（買付余力）
    // 精算予定一覧表（5営業日後）
    this.afterFivedaysSettlementDate = '' // 5営業日後（受渡日）
    this.afterFivedaysDepositIncludingMrf = 0 // 5営業日後（預り金(MRF含む)）
    this.afterFivedaysMargin = 0 // 5営業日後（保証金現金）
    this.afterFivedaysYenDeposit = 0 // 5営業日後（預り金）
    this.afterFivedaysDepositAmount = 0 // 5営業日後（入金額）
    this.afterFivedaysPayment = 0 // 5営業日後（支払額）
    this.afterFivedaysUncontractedCreditSettlementLoss = 0 // 5営業日後（未約定信用決済損）
    this.afterFivedaysUncontractedPurchaseOrderAmount = 0 // 5営業日後（未約定買注文額）
    this.afterFivedaysWithdrawalTransferInstructions = 0 // 5営業日後（出金・振替指示額）
    this.afterFivedaysAmountReceived = 0 // 5営業日後（受取額）
    this.afterFivedaysDayTrading = 0 // 5営業日後（受取額（日計り分））
    this.afterFivedaysSbiHybridDepositSettlementAmount = 0 // 5営業日後(SBIハイブリッド預金精算額)
    this.afterFivedaysTotalBalance = 0 // 5営業日後（残高合計額）
    this.afterFivedaysOurCompanyTotalBalance = 0 // 5営業日後(残高（当社）)
    this.afterFivedaysSbiHybridDepositSettlementTotalBalance = 0 // 5営業日後(残高(SBIハイブリッド預金))
    this.afterFivedaysSbiHybridDepositSettlementRestrictedAmount = 0 // 5営業日後(SBIハイブリッド預金拘束額)
    this.afterFivedaysMarginTotalBalance = 0 // 5営業日後(残高(保証金現金))
    this.afterFivedaysDpositTotalBalance = 0 // 5営業日後(残高(預り金))
    this.afterFivedaysRequiredSettlementAmountYen = 0 // 5営業日後（必要精算額、必要保証金額）
    this.afterFivedaysYenBuyingPowerGeneralAccount = 0 // 5営業日後（買付余力）
    // 精算予定一覧表（ジュニアNISA口座）（当日）
    this.jrNisaSettlementDate = '' // 当日(JrNISA)（受渡日）
    this.jrNisaDepositIncludingMrf = 0 // 当日(JrNISA)（預り金(MRF含む)）
    this.jrNisaDepositAmount = 0 // 当日(JrNISA)（入金額）
    this.jrNisaPayment = 0 // 当日(JrNISA)（支払額）
    this.jrNisaUncontractedPurchaseOrderAmount = 0 // 当日(JrNISA)（未約定買注文額）
    this.jrNisaWithdrawalTransferInstructions = 0 // 当日(JrNISA)（出金・振替指示額）
    this.jrNisaAmountReceived = 0 // 当日(JrNISA)（受取額）
    this.jrNisaDayTrading = 0 // 当日(JrNISA)（受取額（日計り分））
    this.jrNisaPlannedTransferAmount = 0 // 当日(JrNISA)(ジュニアNISA振替予定額（総合口座→ジュニアNISA口座）)
    this.jrNisaTotalBalance = 0 // 当日(JrNISA)(残高合計額)
    this.jrNisaTransferableAmount = 0 // 当日(JrNISA)(ジュニアNISA振替可能額（総合口座→ジュニアNISA口座）)
    this.jrNisaRequiredSettlementAmountYen = 0 // 当日(JrNISA)（必要精算額、必要保証金額）
    this.jrNisaYenBuyingPowerGeneralAccount = 0 // 当日(JrNISA)（買付余力）
    // 精算予定一覧表（ジュニアNISA口座）（1営業日後）
    this.afterOnedayJrNisaSettlementDate = '' // 1営業日後(JrNISA)（受渡日）
    this.afterOnedayJrNisaDepositIncludingMrf = 0 // 1営業日後(JrNISA)（預り金(MRF含む)）
    this.afterOnedayJrNisaDepositAmount = 0 // 1営業日後(JrNISA)（入金額）
    this.afterOnedayJrNisaPayment = 0 // 1営業日後(JrNISA)（支払額）
    this.afterOnedayJrNisaUncontractedPurchaseOrderAmount = 0 // 1営業日後(JrNISA)（未約定買注文額）
    this.afterOnedayJrNisaWithdrawalTransferInstructions = 0 // 1営業日後(JrNISA)（出金・振替指示額）
    this.afterOnedayJrNisaAmountReceived = 0 // 1営業日後(JrNISA)（受取額）
    this.afterOnedayJrNisaDayTrading = 0 // 1営業日後(JrNISA)（受取額（日計り分））
    this.afterOnedayJrNisaPlannedTransferAmount = 0 // 1営業日後(JrNISA)(ジュニアNISA振替予定額（総合口座→ジュニアNISA口座）)
    this.afterOnedayJrNisaTotalBalance = 0 // 1営業日後(JrNISA)(残高合計額)
    this.afterOnedayJrNisaTransferableAmount = 0 // 1営業日後(JrNISA)(ジュニアNISA振替可能額（総合口座→ジュニアNISA口座）)
    this.afterOnedayJrNisaRequiredSettlementAmountYen = 0 // 1営業日後(JrNISA)（必要精算額、必要保証金額）
    this.afterOnedayJrNisaYenBuyingPowerGeneralAccount = 0 // 1営業日後(JrNISA)（買付余力）
    // 精算予定一覧表（ジュニアNISA口座）（2営業日後）
    this.afterTwodaysJrNisaSettlementDate = '' // 2営業日後(JrNISA)（受渡日）
    this.afterTwodaysJrNisaDepositIncludingMrf = 0 // 2営業日後(JrNISA)（預り金(MRF含む)）
    this.afterTwodaysJrNisaDepositAmount = 0 // 2営業日後(JrNISA)（入金額）
    this.afterTwodaysJrNisaPayment = 0 // 2営業日後(JrNISA)（支払額）
    this.afterTwodaysJrNisaUncontractedPurchaseOrderAmount = 0 // 2営業日後(JrNISA)（未約定買注文額）
    this.afterTwodaysJrNisaWithdrawalTransferInstructions = 0 // 2営業日後(JrNISA)（出金・振替指示額）
    this.afterTwodaysJrNisaAmountReceived = 0 // 2営業日後(JrNISA)（受取額）
    this.afterTwodaysJrNisaDayTrading = 0 // 2営業日後(JrNISA)（受取額（日計り分））
    this.afterTwodaysJrNisaPlannedTransferAmount = 0 // 2営業日後(JrNISA)(ジュニアNISA振替予定額（総合口座→ジュニアNISA口座）)
    this.afterTwodaysJrNisaTotalBalance = 0 // 2営業日後(JrNISA)(残高合計額)
    this.afterTwodaysJrNisaTransferableAmount = 0 // 2営業日後(JrNISA)(ジュニアNISA振替可能額（総合口座→ジュニアNISA口座）)
    this.afterTwodaysJrNisaRequiredSettlementAmountYen = 0 // 2営業日後(JrNISA)（必要精算額、必要保証金額）
    this.afterTwodaysJrNisaYenBuyingPowerGeneralAccount = 0 // 2営業日後(JrNISA)（買付余力）
    // 精算予定一覧表（ジュニアNISA口座）（3営業日後）
    this.afterThreedaysJrNisaSettlementDate = '' // 3営業日後(JrNISA)（受渡日）
    this.afterThreedaysJrNisaDepositIncludingMrf = 0 // 3営業日後(JrNISA)（預り金(MRF含む)）
    this.afterThreedaysJrNisaDepositAmount = 0 // 3営業日後(JrNISA)（入金額）
    this.afterThreedaysJrNisaPayment = 0 // 3営業日後(JrNISA)（支払額）
    this.afterThreedaysJrNisaUncontractedPurchaseOrderAmount = 0 // 3営業日後(JrNISA)（未約定買注文額）
    this.afterThreedaysJrNisaWithdrawalTransferInstructions = 0 // 3営業日後(JrNISA)（出金・振替指示額）
    this.afterThreedaysJrNisaAmountReceived = 0 // 3営業日後(JrNISA)（受取額）
    this.afterThreedaysJrNisaDayTrading = 0 // 3営業日後(JrNISA)（受取額（日計り分））
    this.afterThreedaysJrNisaPlannedTransferAmount = 0 // 3営業日後(JrNISA)(ジュニアNISA振替予定額（総合口座→ジュニアNISA口座）)
    this.afterThreedaysJrNisaTotalBalance = 0 // 3営業日後(JrNISA)(残高合計額)
    this.afterThreedaysJrNisaTransferableAmount = 0 // 3営業日後(JrNISA)(ジュニアNISA振替可能額（総合口座→ジュニアNISA口座）)
    this.afterThreedaysJrNisaRequiredSettlementAmountYen = 0 // 3営業日後(JrNISA)（必要精算額、必要保証金額）
    this.afterThreedaysJrNisaYenBuyingPowerGeneralAccount = 0 // 3営業日後(JrNISA)（買付余力）
    // 精算予定一覧表（ジュニアNISA口座）（4営業日後）
    this.afterFourdaysJrNisaSettlementDate = '' // 4営業日後(JrNISA)（受渡日）
    this.afterFourdaysJrNisaDepositIncludingMrf = 0 // 4営業日後(JrNISA)（預り金(MRF含む)）
    this.afterFourdaysJrNisaDepositAmount = 0 // 4営業日後(JrNISA)（入金額）
    this.afterFourdaysJrNisaPayment = 0 // 4営業日後(JrNISA)（支払額）
    this.afterFourdaysJrNisaUncontractedPurchaseOrderAmount = 0 // 4営業日後(JrNISA)（未約定買注文額）
    this.afterFourdaysJrNisaWithdrawalTransferInstructions = 0 // 4営業日後(JrNISA)（出金・振替指示額）
    this.afterFourdaysJrNisaAmountReceived = 0 // 4営業日後(JrNISA)（受取額）
    this.afterFourdaysJrNisaDayTrading = 0 // 4営業日後(JrNISA)（受取額（日計り分））
    this.afterFourdaysJrNisaPlannedTransferAmount = 0 // 4営業日後(JrNISA)(ジュニアNISA振替予定額（総合口座→ジュニアNISA口座）)
    this.afterFourdaysJrNisaTotalBalance = 0 // 4営業日後(JrNISA)(残高合計額)
    this.afterFourdaysJrNisaTransferableAmount = 0 // 4営業日後(JrNISA)(ジュニアNISA振替可能額（総合口座→ジュニアNISA口座）)
    this.afterFourdaysJrNisaRequiredSettlementAmountYen = 0 // 4営業日後(JrNISA)（必要精算額、必要保証金額）
    this.afterFourdaysJrNisaYenBuyingPowerGeneralAccount = 0 // 4営業日後(JrNISA)（買付余力）
    // 精算予定一覧表（ジュニアNISA口座）（5営業日後）
    this.afterFivedaysJrNisaSettlementDate = '' // 5営業日後(JrNISA)（受渡日）
    this.afterFivedaysJrNisaDepositIncludingMrf = 0 // 5営業日後(JrNISA)（預り金(MRF含む)）
    this.afterFivedaysJrNisaDepositAmount = 0 // 5営業日後(JrNISA)（入金額）
    this.afterFivedaysJrNisaPayment = 0 // 5営業日後(JrNISA)（支払額）
    this.afterFivedaysJrNisaUncontractedPurchaseOrderAmount = 0 // 5営業日後(JrNISA)（未約定買注文額）
    this.afterFivedaysJrNisaWithdrawalTransferInstructions = 0 // 5営業日後(JrNISA)（出金・振替指示額）
    this.afterFivedaysJrNisaAmountReceived = 0 // 5営業日後(JrNISA)（受取額）
    this.afterFivedaysJrNisaDayTrading = 0 // 5営業日後(JrNISA)（受取額（日計り分））
    this.afterFivedaysJrNisaPlannedTransferAmount = 0 // 5営業日後(JrNISA)(ジュニアNISA振替予定額（総合口座→ジュニアNISA口座）)
    this.afterFivedaysJrNisaTotalBalance = 0 // 5営業日後(JrNISA)(残高合計額)
    this.afterFivedaysJrNisaTransferableAmount = 0 // 5営業日後(JrNISA)(ジュニアNISA振替可能額（総合口座→ジュニアNISA口座）)
    this.afterFivedaysJrNisaRequiredSettlementAmountYen = 0 // 5営業日後(JrNISA)（必要精算額、必要保証金額）
    this.afterFivedaysJrNisaYenBuyingPowerGeneralAccount = 0 // 5営業日後(JrNISA)（買付余力）
    // フラグ類
    this.generalAccountFlag = '' // 一般総合口座フラグ
    this.jrGeneralAccountFlag = '' // ジュニア総合口座フラグ
    this.jrNisageneralAccountFlag = '' // ジュニアNISA口座フラグ
    this.sbiHybridFlag = '' // SBIハイブリッドフラグ
  }
}
